import type { CanvasInterface } from '@dscvr-one/canvas-client-sdk'

export const validateHostMessage = async <T>(message: CanvasInterface.HostMessage) => {
  // TODO: connect with graphql
  return true
}
