# @dscvr-one/canvas-solana-interface

This package defines the schema and typescript types used for the client to host protocol for DSCVR canvas.

## Installation

Install with your favorite package manager:

```bash
npm install @dscvr-one/canvas-solana-interface
```

yarn

```bash
yarn add @dscvr-one/canvas-solana-interface
```
